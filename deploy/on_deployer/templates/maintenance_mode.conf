{% if maintenance_on %}
set $maintenance on;
{% else %}
set $maintenance off;
{% endif %}

{% if ip_whitelist %}
{% for ip in ip_whitelist %}
{% if ip %}
if ($remote_addr = {{ip}}) {
   set $maintenance off;
}
{% endif %}
{% endfor %}
{% endif %}
