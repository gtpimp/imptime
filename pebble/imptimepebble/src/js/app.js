var UI = require('ui');
// var ajax = require('ajax');
// var Vector2 = require('vector2');
// var Accel = require('ui/accel');
// var Vibe = require('ui/vibe');

var card = new UI.Card({
    title: 'ImpTime',
    body: 'Press the button then speak',
    scrollable: true });
card.show();


